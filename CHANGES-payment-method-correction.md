# ميزة جديدة: تصحيح طريقة الدفع على فاتورة محفوظة

## المشكلة
بعد إتمام البيع وحفظ الفاتورة، لم تكن هناك أي طريقة لتصحيح طريقة الدفع
المسجّلة عليها (مثال: اتسجّلت "بطاقة" ثم تبيّن أنها فاضية/مرفوضة، والعميل
دفع نقداً فعلياً). الحل الوحيد كان إرجاع الفاتورة بالكامل وإعادة بيعها من
الصفر — إجراء ثقيل وغير دقيق (يفتح فاتورة جديدة برقم مختلف، ويزعج تسلسل
الفواتير والتقارير).

## الحل
خيار **"تصحيح طريقة الدفع"** جديد في مودال تفاصيل الفاتورة بصفحة التقارير
(تبويب "الفواتير والديون")، بالشروط التالية:

- يظهر فقط لـ **مدير/أدمن** (الصفحة كلها محصورة بهذين الدورين أصلاً)، وفقط
  على فاتورة بحالة `completed` أو `partially_refunded`.
- **سبب إلزامي** لكل تصحيح (نص قصير).
- **لا يغيّر** `subtotal`/`tax_total`/`discount_total`/`grand_total` إطلاقاً —
  فقط توزيع الدفع (`payment_method`/`cash_amount`/`card_amount`/`due_amount`).
- **لا يُعدَّل** أي صف قديم في `payment_transactions` — يبقى أثراً تاريخياً
  لما حصل فعلياً وقت البيع. بدلاً من ذلك تُضاف حركة "تصحيح" جديدة موصوفة
  بوضوح (`تصحيح دفع: <قديم> → <جديد>`).
- **تسوية دين العميل تلقائياً**: لو التحويل من/إلى "آجل"، يُحدَّث
  `customer_ledger` ورصيد العميل بفرق الدين (net delta)، مع تحذير بالواجهة
  لو أصبح رصيد العميل سالباً (يعني له رصيد دائن يحتاج مراجعة يدوية). التحويل
  إلى "آجل" مرفوض من الأساس لو لا يوجد عميل مرتبط بالفاتورة.
- **سجل تدقيق كامل**: `action: 'sale_payment_method_corrected'` بمستوى
  `warning`، ويحفظ الطريقة القديمة/الجديدة، المبالغ القديمة/الجديدة، السبب،
  مين نفّذ التصحيح، وهل الوردية المرتبطة بالفاتورة كانت مقفولة وقت التصحيح.

## تنبيه مهم — لا يُصلَح تلقائياً
"الكاش المفروض يكون بالصندوق الآن" بصفحة التقارير يُحسب مباشرة من
`sales.cash_amount` للوردية النشطة (`computeExpectedCash`). لو الوردية
المرتبطة بالفاتورة **مقفولة أصلاً** وقت التصحيح، تقرير إقفال تلك الوردية
(`expected_cash` المحفوظ وقت الإقفال) **لا يُعاد حسابه** — يبقى فرق موثّق
فقط بسجل التدقيق. هذا سلوك متوقّع (لا يمكن تعديل تاريخ مقفول بصمت)، وتظهر
الواجهة تنبيهاً صريحاً للمستخدم في هذه الحالة تحديداً.

## نطاق مقصود لم يُشمَل الآن
- **"رصيد المتجر" (`store_credit`)** غير مشمول بهذا التصحيح — يتطلب تحققاً
  مختلفاً (كفاية الرصيد + `store_credit_ledger`) يستحق تصميماً منفصلاً.
- لا يوجد تحقق تلقائي من قواعد فوترة ضريبية خاصة ببلد معيّن (وحدة
  `fiscalization/index.js` عامة/غير مفعّلة حالياً بهذا المشروع) — لو فُعِّل
  مزوّد ضريبي حقيقي لاحقاً، يُنصح بمراجعة ما إذا كان "تعديل بعد الحفظ"
  مسموحاً به أصلاً أو يتطلب فاتورة تصحيحية/دائنة منفصلة.

## الملفات المعدّلة
- `database/db.js` — دالة `correctSalePaymentMethod` (معاملة `db.transaction`
  واحدة: تحقق + تحديث الفاتورة + تسوية دين العميل + حركة دفع تصحيحية + سجل
  تدقيق).
- `main.js` — قناة `sales:correctPaymentMethod` (صلاحية `management.access`
  تُتحقق أيضاً هنا دفاعاً بالعمق، وليس فقط بإخفاء الزر بالواجهة).
- `preload.js` — تعريض `window.api.sales.correctPaymentMethod`.
- `renderer/pages/reports.js` — زر وفورم "تصحيح طريقة الدفع" داخل مودال
  تفاصيل الفاتورة، مع تأكيد صريح قبل الحفظ وتحديث التقارير بعده.
- `renderer/style.css` — تنسيق بسيط للفورم الجديد.

## ملاحظة للنشر
`CORE_SHA256.txt` (بصمة سلامة ملفات الإصدار) يحتاج إعادة توليد بعد هذا
التعديل عبر أداة الإصدار المعتادة (`tools/release-manifest.js` وما شابه)
قبل تعبئة الإصدار التالي — التعديل الحالي غيّر `database/db.js` و`main.js`
و`preload.js`، وهي من الملفات المشمولة بالبصمة.

## اختبار
تعذّر تشغيل اختبارات تكاملية فعلية داخل هذه الجلسة لعدم توفر اتصال شبكة
لتثبيت `node_modules` (بما فيها `better-sqlite3`). تم التحقق من سلامة بناء
الجملة (`node --check`) لكل الملفات المعدّلة فقط. **يُنصح بشدة بتشغيل
`tools/full-regression.js` (أو ما يعادله من سكربتات الاختبار الموجودة) على
جهاز فيه `npm install` كامل قبل اعتماد هذا التعديل.**
