# ميزة جديدة: حذف/تعطيل مستخدم من شاشة "المستخدمون"

## المشكلة
لم تكن هناك أي وسيلة لحذف مستخدم مضاف بالخطأ — لا زر حذف بالواجهة، ولا حتى IPC/دالة
حذف بالخلفية. هذه لم تكن "علّة" بمعنى كود معطوب، بل ميزة لم تُبنَ من الأساس.

## لماذا لم يكن حذفاً نهائياً بسيطاً كافياً
جدول `users` مرتبط بعشرات الجداول (المبيعات، الورديات، الدفعات، حركات الصندوق، سجل
التدقيق، الرواتب القديمة...) عبر قيود مرجعية (foreign keys) مُفعَّلة فعلياً بقاعدة البيانات
(`PRAGMA foreign_keys = ON`). حذف مستخدم له أي عمليات سابقة كان سيكسر تلك السجلات أو
يفشل بخطأ قاعدة بيانات غير مفهوم للمستخدم.

## الحل المطبَّق
- زر **"حذف"** جديد بجانب كل مستخدم (لا يظهر لحسابك أنت المسجَّل دخوله حالياً).
- عند الحذف:
  - إن لم يكن للمستخدم أي سجلات سابقة → **حذف نهائي فعلي**.
  - إن كان مرتبطاً بسجلات سابقة (مبيعات/ورديات/رواتب/تدقيق...) → **تعطيل تلقائي بدلاً من
    الحذف** (لا يستطيع تسجيل الدخول بعدها)، مع رسالة واضحة للمدير تشرح ما حدث بالضبط
    وتوضح أن السبب هو وجود سجلات تاريخية، بدل تحويل صامت لا يفهمه أحد.
  - **لا يمكن حذف حسابك أنت** الذي تستخدمه حالياً لتسجيل الدخول.
  - **لا يمكن حذف أو تعطيل أو إنزال دور آخر حساب "مدير عام" مفعّل في الفرع** — هذا الفحص
    أصبح أيضاً مطبَّقاً من جهة الخادم (main/db) وليس فقط تحذيراً بالواجهة كما كان سابقاً،
    لمنع قفل النظام بالكامل بلا أي حساب قادر على الوصول إلى الإعدادات/الرواتب/التدقيق.
- ملفات معدّلة: `database/db.js`, `main.js`, `preload.js`, `renderer/pages/users.js`,
  `renderer/pages/users.html`.
- اختبار وظيفي جديد: `tools/users-delete-regression.js` (5 سيناريوهات) — مربوط الآن ضمن
  `tools/full-regression.js` حتى يُفحص تلقائياً بكل مرة مستقبلاً.

## فحوصات مُشغَّلة (كلها ناجحة)
- `node tools/sale-readiness.js`
- `node tools/full-regression.js` (112 ملف JS + كل فحوصات الرواتب/التزامن/الأمان + اختبار حذف المستخدمين الجديد)
- `node tools/users-delete-regression.js` منفرداً
- `node tools/release-metadata-regression.js`
