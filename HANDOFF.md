# تقرير تسليم — Nexora POS، دفعة تعميق المحاسبة (Batch 1)

كُتب هذا الملف عشان أي مطور يكمل من هون بدون ما يحتاج يرجع لمحادثة سابقة.
راجعه مع `CHANGES-accounting-engine-batch1.md` (نفس المجلد) اللي فيه تفاصيل تقنية أدق لكل قيد محاسبي.

## الهدف العام (اللي طلبه صاحب المشروع)
تطوير Nexora POS تدريجيًا "على دفعات" ليصير أقوى من Odoo **بنطاقه كنقطة بيع/تجزئة متخصصة**
(مو تحويله لمنصة ERP ويب متعددة المستأجرين — تم الاتفاق صراحة على هذا الاتجاه، مو تحويل كامل).
الدفعة الأولى المتفَق عليها: تعميق **المحاسبة** لأنها كانت الفجوة الأكبر.

## الوضع قبل هذه الدفعة (باختصار)
كانت جداول المحاسبة موجودة بس **فقط المبيعات** كانت تُرحَّل لدفتر الأستاذ. المشتريات، تسديد
ديون الموردين، تحصيل ديون العملاء، المرتجعات، والرواتب — ما كانت تلمس المحاسبة إطلاقًا. يعني
ميزان المراجعة ما كان ممكن يطلع متوازن فعليًا، ولا كان فيه أي تقرير مالي حقيقي (ميزان مراجعة/
دخل/مركز مالي) ولا آلية قفل فترات.

## ✅ اللي خلص فعليًا بهذه الدفعة
1. **قفل الفترات المحاسبية** حقيقي — أي قيد جديد بتاريخ ضمن فترة مقفلة يُرفض تلقائيًا من أي
   مصدر (`insertPostedJournalEntry` هي نقطة المرور الوحيدة).
2. **4 تقارير مالية جديدة كليًا**: ميزان مراجعة، قائمة دخل، مركز مالي (مع ترحيل صافي الدخل
   كأرباح مرحّلة تلقائيًا)، دفتر أستاذ لكل حساب.
3. **ربط الترحيل التلقائي** بخمس عمليات كانت غائبة تمامًا عن المحاسبة: استلام فاتورة شراء،
   تسديد دين مورد، تحصيل دين عميل، المرتجعات (بعكس دقيق للإيراد+الضريبة+تكلفة البضاعة)،
   وصرف الرواتب.
4. **ترحيلة قاعدة بيانات جديدة (v17)** — بدون لمس ترحيلة v3 المنشورة أصلاً (لأنها محسوبة
   checksum لجسمها؛ أي تعديل عليها يكسر ترقية أي قاعدة بيانات مستخدم شغّالة فعلاً. **نقطة
   حرجة لازم ينتبهلها أي مطور جديد: أي ترحيلة برقم موجود بمصفوفة `[N, 'name', fn]` بداخل
   `runMigrations()` بملف `database/db.js` — جسمها محرّم يتغيّر بعد النشر، ترحيلة جديدة دايمًا
   برقم أعلى**).
5. **واجهة مستخدم كاملة جديدة**: `renderer/pages/accounting.html` + `accounting.js` (خمس
   تبويبات)، مربوطة بالقائمة الجانبية بكل صفحات التطبيق (14 ملف HTML)، محمية بصلاحيات مدير/أدمن
   بالخلفية (`main.js`) مو بس بإخفاء الأزرار.
6. فحص `node --check` ناجح على كل الملفات المعدّلة (`database/db.js`, `main.js`, `preload.js`,
   `renderer/pages/accounting.js`).

## ⚠️ الأهم: ولا شيء من هذا اتفحص بالتشغيل الفعلي
**البيئة اللي اشتغلت فيها ما فيها اتصال إنترنت** لتثبيت `better-sqlite3` وباقي `node_modules`،
فكل شغلي كان **مراجعة يدوية دقيقة + `node --check` (فحص نحوي فقط)**. هذا أهم شيء لازم
المطور الجديد يعمله **قبل أي اعتماد**:
1. `npm install` ثم تشغيل التطبيق فعليًا.
2. عمل بيع + شراء + مرتجع + تسديد دين عميل/مورد + صرف راتب تجريبي، والتأكد إن ميزان
   المراجعة (`accounting.html`) يطلع **متوازن** (`balanced: true`).
3. تشغيل `tools/full-regression.js` أو مكافئه — **خصوصًا** أي ملف بالاسم
   `payments-purchases-regression.js` إذا موجود عندكم.
4. إعادة توليد `CORE_SHA256.txt` قبل أي إصدار (لاحظنا هذا من أول جلسة، لسه ما انعمل).

## ✅ تحديث: تم فعليًا تشغيل واختبار كل هذا (مراجعة ثانية لاحقة)
راجع مطوّر تاني (مع اتصال إنترنت هالمرة) الكود فعليًا — مو بس نظريًا. النتيجة: **كل ادعاءات
هذا الملف صحيحة ومطابقة للكود الفعلي**. ثبّت `better-sqlite3` وكتب اختبار حقيقي
(`tools/claude-accounting-e2e.js`) نفّذ فيه الست عمليات كلها (بيع، شراء، تسديد دين مورد،
تحصيل دين عميل، مرتجع، صرف راتب) على قاعدة بيانات حقيقية — **ميزان المراجعة والمركز المالي
طلعا متوازنين فعليًا (`balanced: true`)**. قفل الفترة رفض قيدًا فعليًا واشتغل صح. شغّل أيضًا
`tools/payments-purchases-regression.js` الموجود بالمشروع (5/5 نجح).

**بالمقابل، لقى واصلح خللين حقيقيين (قديمين، مو من هذه الدفعة، لكن بنفس الدوال اللي هذه
الدفعة رحّلت فيها للمحاسبة)**:
1. `recordPayrollPayment` كانت ترمي خطأ دايمًا لو ما مُرِّر لها `paymentDate` صراحة (الواجهة
   ما كانت متأثرة لأنها دايمًا بتبعت التاريخ، بس أي استدعاء برمجي كان ينكسر). **مُصلَّح**.
2. `addPayrollV2Transaction` كانت ترمي `ReferenceError: unit is not defined` **دائمًا** —
   يعني تسجيل غياب/مكافأة/خصم/إضافي/ساعات عمل لأي موظف كان **فاشلاً 100% من المرات** قبل
   هذا الإصلاح. **مُصلَّح** (متغير `unit` كان ناقص تعريفه بالدالة).

تفاصيل تقنية كاملة للاثنين + ملاحظة عن اختبار قديم (`payroll-v2-integration-regression.js`)
محتاج تحديث لأنه بيستخدم API سلف قديم — بـ`CHANGES-accounting-engine-batch1.md`.
اختبار المحاسبة الفعلي مرفق بـ`tools/claude-accounting-e2e.js` — شغّله في أي وقت للتأكد.

## 🔧 قيود معروفة / مرشحة للدفعة القادمة (موثقة بالتفصيل بـ CHANGES-accounting-engine-batch1.md)
- الرواتب تُرحَّل على أساس **نقدي** (وقت الصرف)، مو استحقاقي (وقت اكتساب الراتب). محاسبة
  استحقاقية كاملة تحتاج حساب "رواتب مستحقة" (liability) — نطاق أكبر مؤجل.
- سلف الموظفين (صرف واسترداد) ما زالت **خارج** الترحيل المحاسبي بالكامل (لأن صرف السلفة
  الأصلي أصلاً ما كان مُرحَّلاً من قبل — تركتها متسقة بدل ما أرحّل نص العملية بس).
- رابط "المحاسبة" بالقائمة الجانبية نص عربي ثابت — ما اتضاف له مفاتيح ترجمة (en/tr)، بس
  يتدهور بأمان (يبقى يعرض العربي).
- **لا يوجد شاشة لإضافة قيد يدوي** (مثلاً إيجار أو مصروف نقدي مباشر). الدوال الخلفية
  (`postJournalEntry`, `createAccountingAccount`) موجودة وجاهزة، بس بدون واجهة — مرشح ممتاز
  كأول شيء بالدفعة الجاية.

## 🗺️ الخطة الكاملة المتفَق عليها (دفعات لسه ما بدأت)
كانت الخطة المقترحة (بعد الدفعة 1 - المحاسبة):
2. مخزون متقدم: مستودعات متعددة، طرق تقييم (FIFO/متوسط مرجّح)، تحويلات، جرد دوري.
3. مشتريات/موردين: عروض أسعار، مقارنة موردين، استلام جزئي، فوترة مطابقة.
4. CRM/مبيعات B2B: عملاء محتملون، عروض أسعار، عقود.
5. توسعة API + نظام "وحدات" (plugins) حتى تصير الإضافات المستقبلية plug-in مو تعديل مباشر
   بـ `db.js` (اللي صار كبير جدًا — حاليًا حوالي 6000+ سطر بملف واحد).

## ملاحظة سياقية من جلسة سابقة (مراجعة ميزة تصحيح طريقة الدفع)
قبل هذه الدفعة، راجعت ميزة "تصحيح طريقة الدفع" (`correctSalePaymentMethod`) اللي طوّرها مطور
سابق، ولقيت فجوتين صغيرتين لسه (على حد علمي) ما انصلحوا:
1. `renderer/pages/customers.js` سطر ~80: كشف حساب العميل يعرض أي `entry_type` غير
   `credit_sale` كـ"تسديد" — بما فيها `credit_correction` (ممكن تكون زيادة دين فعليًا، مش تسديد).
2. `getDebtAging()` بـ`db.js`: حساب "أقدم دين" يعتمد فقط على `entry_type='credit_sale'`،
   فلو عميل دينه الوحيد جاء من تصحيح دفع (`credit_correction`)، هيظهر برصيد موجب بتقرير
   أعمار الديون لكن بدون تاريخ "أقدم دين".

بالتوفيق للمطور اللي رح يكمل 🙏

## 🔄 تحديث لاحق: قيد يدوي + 3 أخطاء حقيقية مُصلَحة (راجع `CHANGES-accounting-manual-entry-and-fixes.md`)
جلسة تالية بنت شاشة "قيد يدوي" المقترحة أعلاه، ووجدت أثناء التنفيذ الفعلي (مو `node --check` فقط)
3 أخطاء حقيقية غير متعلقة بها مباشرة — تم إصلاحها كلها واختبارها فعلياً بتشغيل حقيقي:
1. **`preload.js` كان يكسر صفحة `accounting.html` بالكامل** — مفتاح `accounting` معرَّف مرتين
   بنفس كائن `exposeInMainWorld`، والتعريف الثاني كان يُلغي الأول بصمت (يحذف
   `trialBalance/incomeStatement/balanceSheet/...`). هذا الخلل كان موجوداً منذ الدفعة 1 نفسها
   ولم يكتشفه أي اختبار سابق لأن ولا واحد منها يمر عبر `preload.js`.
2. **تسجيل الإضافي (overtime) كان مكسوراً 100%** من الواجهة الفعلية — `addPayrollV2Transaction`
   كانت ترفض `amount=0` لنوع `overtime`، بينما الواجهة ترسل `amount:0` عمداً دائماً لهذا النوع
   (لأن الأجر يُحتسب من الساعات تلقائياً).
3. الاختبار القديم `tools/payroll-v2-integration-regression.js` (المذكور أعلاه كمعلَّق) تم تحديثه
   فعلياً واختباره بنجاح — كان يحتاج تعديلات أكثر مما بدا (السلف + `start_date` المعتمد على
   `hire_date` + احتساب الإضافي من الساعات، ثلاثتها ميزات أُضيفت بعد كتابة الاختبار الأصلي).

تفاصيل تقنية كاملة ونتائج كل اختبار شُغِّل فعلياً بـ`CHANGES-accounting-manual-entry-and-fixes.md`.

