# ملخص إصلاحات v0.52.12

هذا الملف يشرح كل تعديل مرفق بهذا الحزمة (`tools/`). طبّقهم بالترتيب عبر:

```powershell
node tools/apply-all-fixes.js
```

كل سكربت آمن ومحمي: بيتحقق من النص المتوقع قبل ما يلمس أي ملف، وبيتخطى بصمت لو
الإصلاح مطبّق أصلاً (تقدر تعيد تشغيله براحتك بدون خوف).

## 1) fix-v19-checksum.js
إرجاع دالة `migratePayrollAccrualV19` بـ`database/db.js` لنصها المنشور الأصلي
(حذف فلترة `currentMonthKeyGuard` اللي انضافت بالغلط داخل الهجرة بعد نشرها).
هذا كان يسبب "Migration v19 was modified after publication; checksum mismatch"
ويمنع التطبيق من الإقلاع كليّاً عند أي عميل شغّل v19 قبل هذا التعديل. migration
v20 الموجودة أصلاً بتصحح أي استحقاق خاطئ لشهر مستقبلي، فمفيش فقدان بالسلوك.

## 2) payroll-accrual-v19-regression.js (استبدال كامل)
إصلاح باغ منفصل بملف الاختبار نفسه: كان ناقص تمويه موديول `electron` (نفس نمط
`loyalty-redemption-regression.js`)، فكان يفشل تحت `node` العادي، وإذا اشتغل
عبر Electron الحقيقي كان يكتب بيانات اختبار على قاعدة البيانات الحقيقية.

## 3) fix-tax-duplicate-key.js
دمج مفتاح `tax` المكرر بـ`preload.js` (كان معرّف مرتين، فالتعريف الثاني كان
يمحي `defaultRate` بصمت) — سبب خطأ `window.api.tax.defaultRate is not a function`.

## 4) preload-duplicate-key-regression.js (ملف جديد)
فحص دائم يكتشف أي مفتاح مستوى أول مكرر بكائن `window.api` بـ`preload.js`
مستقبلاً، قبل ما يوصل لأي عميل.

## 5) fix-qty-buffer-arabic-digits.js
خانة "الكمية" بشاشة الكاشير (`qtyBufferInput`) كانت `type="number"`، فكانت
ترفض الأرقام العربية/الفارسية بصمت (لا يظهر أي شيء عند الكتابة بلوحة مفاتيح
عربية). تحويلها لـ`type="text" inputmode="numeric"` + قراءتها بـ`parseLocaleNumber`.

## 6) fix-arabic-digit-inputs.js
نفس مشكلة رقم 5، بس بـ~35 خانة تانية بكل شاشات التطبيق (دفع الكاشير المختلط،
الاستلام النقدي، تقسيم الفاتورة، المنتجات، الموردين، المخزون، الرواتب،
الإعدادات) — بما فيها حقول مولّدة ديناميكياً بـ`payroll.js`/`table-order.js`
ما كانت تظهر بفحص ملفات HTML الثابتة.

## 7) number-input-arabic-digit-regression.js (ملف جديد)
فحص دائم يكتشف أي حقل `type="number"` جديد (ثابت أو مولّد ديناميكياً بالكود)
قبل ما يوصل لأي عميل — بدل ما ننتظر بلاغ خطأ.

## 8) fix-category-tabs-always-show.js
تبويبات الفئات بشاشة الكاشير كانت تختفي بالكامل لو ولا فئة عندها صورة مرفوعة.
الآن كل فئة مضافة من الإعدادات تظهر كتبويب دايماً — بصورتها لو موجودة، أو باسمها
نصياً (أول حرف كأيقونة) لو ما في صورة.

---

بعد تشغيل `apply-all-fixes.js` بنجاح، شغّل الفحوصات المذكورة بآخر مخرجاته،
وبعدها `npm run check` الشامل قبل أي بناء أو نشر.
