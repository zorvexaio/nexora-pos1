# إصلاح: شاشة الدخول لا تعرض شعار/اسم المتجر (انقطاع الهوية البصرية)

## الخلل
- شاشة splash تعرض شعار واسم المتجر المخصَّص (إن كان محفوظاً بالإعدادات) — `splash.js`.
- داخل التطبيق، شريط العنوان العلوي يعرض نفس الشعار/الاسم — `common.js: applyRuntimeBranding()`.
- **شاشة الدخول (login.html) وحدها لم تكن تطبّق هذا إطلاقاً**: لا تحمّل `common.js`
  ولا كان لديها أي منطق مكافئ، فتعرض دائماً الاسم العام "نظام نقاط البيع" بلا أي شعار.

النتيجة: المستخدم يرى شعار متجره في splash، ثم يختفي فجأة بشاشة الدخول، ثم يعود بالداخل —
انقطاع بصري غير مقصود لهوية العلامة التجارية للمتجر.

## الإصلاح
- `renderer/login.html`: أضيف صف شعار فوق عنوان البطاقة (`#loginBrandMark` بنفس تصميم
  `.brand-mark` المستخدم بشريط التطبيق، مكبَّراً قليلاً)، وأُعطي عنوان البطاقة معرّفاً
  (`#loginBrandTitle`) ليمكن استبدال نصّه.
- `renderer/style.css`: قواعد `.login-logo-row` لعرض الشعار بحجم مناسب لبطاقة الدخول.
- `renderer/login.js`: دالة `applyLoginBranding()` جديدة، تستخدم نفس واجهة
  `window.api.branding.get()` التي يستخدمها `splash.js` بالضبط (بلا أي اعتماد على
  `common.js`) — تستبدل الاسم والشعار إن كانا محفوظين، وتتجاهل الفشل بصمت (كما في
  splash) إن لم يكن أي منهما محفوظاً بعد.
