# دمج: إصلاح اقتران الكرسون + رسائل أخطاء المزامنة + محاسبة المخزون

## 1) اقتران جهاز الكرسون (server/sync-server.js)
- شاشة الكرسون كانت تطلب 4 أرقام بينما التطبيق الرئيسي يولّد رمزاً من 8 أرقام → لا يمكن الاقتران أبداً. أصبحت 8 أرقام.
- `require('../database/db')` أصبح كسولاً (داخل `getAppDb()`) لتجنّب تحميل قاعدة البيانات قبل `app.whenReady()` (مشكلة safeStorage على ويندوز).

## 2) رسائل أخطاء المزامنة (database/sync-client.js)
- دالة `describeNetworkError` تحوّل "fetch failed" إلى رسالة عربية واضحة حسب السبب الحقيقي.

## 3) محاسبة المخزون (database/db.js)
- ترحيل المخزون الافتتاحي للمنتج الجديد (من 3000 إلى 1300).
- ترحيل تسويات المخزون اليدوية إلى حساب 5900 Inventory Adjustments.
- إنشاء صفوف المخزون الجديدة بتكلفة المنتج الحقيقية بدل 0 (إنشاء منتج، استيراد CSV، تعديل منتج، تسوية).
- إصلاح تاريخ النهاية في الميزانية/ميزان المراجعة/دفتر الأستاذ (كانت تستبعد قيود اليوم نفسه).
- ترحيلة جديدة **v23** (`inventory-account-correction-v23`) تصحح تكلفة الصفوف القديمة (0) وتطابق رصيد حساب المخزون مع القيمة الفعلية.
  (CURRENT_SCHEMA_VERSION = 23؛ سُجّلت بعد v22 الموجودة أصلاً.)
- تحديث `tools/migration-chain-regression.js` و `tools/v0.9-regression.js` ليتوافقا مع التغييرات.
