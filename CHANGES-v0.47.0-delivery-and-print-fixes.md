# نسخة 0.47.0 — إصلاح الطباعة الصامتة + توصيل احترافي بتسعير أوتوماتيكي

## 1) إصلاح فشل الطباعة الصامت (السبب الحقيقي وراء "الطابعة لا تعمل أبداً")
- كل نداءات الطباعة التلقائية (`autoPrintReceipt`, `autoSendKitchen`) كانت تُستدعى بـ
  `void` فتُهمَل نتيجتها بالكامل — أي فشل (IP خطأ، طابعة أوفلاين، Firewall...) كان
  يُسجَّل في سجل التدقيق فقط بدون أي تنبيه يصل للكاشير.
- الآن نتيجة الطباعة (`printOutcome`) تُرفَق برد كل عملية بيع/إغلاق طاولة، والواجهة
  تعرض تحذيراً فورياً (⚠️) يوضّح سبب الفشل الحقيقي.
- `'delivery'` أُضيفت لقائمة أنواع الطلبات التي تُرسِل تذكرة مطبخ تلقائياً (كانت
  مستبعدة بالكامل من قبل، فطلبات التوصيل المباشرة من شاشة الكاشير ما كانت توصل
  للمطبخ إطلاقاً).
- ملفات معدّلة: `main.js`, `renderer/pos.js`, `renderer/pages/table-order.js`.

## 2) تسعير توصيل أوتوماتيكي حسب المسافة
- إعدادات جديدة (شاشة الإعدادات ← تسعير التوصيل): سعر ثابت افتراضي + سعر الكيلومتر.
- شاشة الكاشير: حقل "المسافة (كم)" — إدخال المسافة يحسب رسوم التوصيل أوتوماتيكياً
  (المسافة × سعر الكيلومتر)، وبدون مسافة تُستخدم القيمة الثابتة الافتراضية.
- ملفات معدّلة: `main.js`, `preload.js`, `database/db.js`, `renderer/pages/settings.html`,
  `renderer/pages/settings.js`, `renderer/index.html`, `renderer/pos.js`.

## 3) ملاحظة عامة على الطلب + وقت تسليم قابل للتعديل
- عمودان جديدان بجدول `sales`: `notes` (ملاحظة الطلب كله) و`delivery_time`
  (NULL = فوري، أو وقت محدد يختاره الكاشير).
- الملاحظة العامة تظهر الآن ببشارة تحذيرية بارزة (خلفية + حد + أيقونة) في تذكرة
  المطبخ والفاتورة، بدل نص صغير غير ملحوظ.
- وقت التسليم (الآن / تحديد وقت) يظهر بوضوح في تذكرة المطبخ (🛵) والفاتورة.
- ملفات معدّلة: `database/db.js`, `renderer/index.html`, `renderer/pos.js`,
  `renderer/kitchen-ticket.js`, `renderer/receipt.js`, `renderer/i18n.js`,
  `renderer/style.css`.

## 4) تكبير أيقونة ملاحظة الصنف في سلة الكاشير
- زر 📝 جنب كل صنف في السلة كان صغيراً بما لا يُلاحظ بسهولة — تم تكبيره بشكل
  متناسق عبر كل مستويات كثافة العرض (density-1 إلى density-4).
- ملف معدّل: `renderer/style.css`.

## 5) السلف من قسم الرواتب أصبحت مرتبطة فعلياً بالصندوق (من نسخة سابقة ضمن هذا التسلسل)
- خيار "دُفعت نقداً من الصندوق الآن؟" عند تسجيل سلفة موظف، يربطها بحركة نقد حقيقية
  في `cash_movements` (صرف عند الإضافة، واسترجاع تلقائي عند الحذف).
- ملفات معدّلة: `database/db.js`, `main.js`, `renderer/pages/payroll.js`,
  `renderer/style.css`.

## ملاحظة اختبار
كل التعديلات اجتازت `node --check` (syntax) على كل ملف معدّل، لكن لم يُشغَّل
التطبيق فعلياً في بيئة التطوير هذه (لا توجد شبكة لتنزيل موديول `better-sqlite3-multiple-ciphers`
الأصلي). يُنصح باختبار السيناريوهات يدوياً على جهاز حقيقي قبل الاعتماد الكامل.
