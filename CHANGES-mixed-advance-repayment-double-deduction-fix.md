# إصلاح: خصم مزدوج للسلفة عند دمج تسديد مباشر + خصم من الراتب لنفس القسط بنفس الشهر

## الخلل
`recordPayrollSalaryAdvanceRecovery()` في `database/db.js` كانت تحسب "المبلغ المخصوم
مسبقاً لقسط هذا الشهر" بالاعتماد فقط على دفعات من نوع `payment_type='salary'`، متجاهلة أي
تسديد مباشر (`payment_type='direct'`) سُدد لنفس القسط بنفس الشهر (مثلاً من شاشة صرف/تسديد
السلف). النتيجة: لو الموظف سدّد قسط الشهر مباشرة (كلياً أو جزئياً) قبل تشغيل مسير الرواتب،
كان مسير الرواتب يحاول خصم القسط الكامل مرة أخرى من الراتب لنفس الشهر — أي خصم مزدوج، وتضخّم
في رصيد "المسترد" يتجاوز فعلياً ما دفعه الموظف، ويجعل رصيد السلفة المتبقي غير متطابق مع
مجموع الأقساط الفعلية.

هذا كان السيناريو الوحيد غير المُختبر من ميزة تسديد السلف (كل مسار — مباشر، وخصم من الراتب —
كان يُختبر بمعزل عن الآخر، ولم يوجد اختبار فعلي يُشغّل المنطق؛ ملف الفحص القديم
`tools/payroll-advance-repayments-regression.js` كان مجرد بحث نصّي عن وجود السطر
`payment_type='salary'` في الكود — وهذا السطر نفسه كان جزءاً من الخلل، فالفحص كان "ينجح"
لأن الخلل موجود، لا رغم غيابه.

## الإصلاح (`database/db.js`)
- استبدال الحساب المُصفّى حسب `payment_type='salary'` بحساب يجمع **كل** المخصص لقسط ذلك
  الشهر (مباشر + راتب معاً)، فيُطرح الصحيح من المبلغ المستحق قبل محاولة الخصم من الراتب.
- نفس الإصلاح طُبّق على توزيع مبلغ الخصم على الأقساط داخل نفس الدالة (كان يتحقق فقط من
  دفعات `salary` سابقة على مستوى القسط الواحد).
- لا تغيير على `repayPayrollAdvance` (التسديد المباشر) — كانت صحيحة أصلاً (تحسب الإجمالي
  بلا تصفية حسب النوع).

## التحقق
- تعذّر تشغيل تطبيق Electron الفعلي (لا يوجد `node_modules`/اتصال شبكة بهذه البيئة لتثبيت
  `better-sqlite3-multiple-ciphers`).
- بدلاً من ذلك، أُعيد بناء نفس استعلامات SQL المستخدمة فعلياً في `db.js` (التسديد المباشر +
  استرداد الراتب) باستخدام `node:sqlite` المدمجة في Node.js (متوفرة محلياً بلا تثبيت)، وشُغّل
  السيناريو المُبلَّغ عنه (سلفة 3000 بثلاث أقساط، تسديد مباشر 1000 يغطي قسط الشهر الأول
  بالكامل، ثم محاولة خصم قسط نفس الشهر من الراتب):
  - **قبل الإصلاح:** القسط أُخذ منه 1000 (مباشر) + 1000 (راتب) = 2000 — خصم مزدوج فعلي.
  - **بعد الإصلاح:** محاولة الخصم من الراتب تُرجع صفراً لأن القسط مسدّد بالفعل؛ الإجمالي
    المسترد يبقى 1000 كما يجب.
- أُضيف اختبار وظيفي حقيقي جديد: `tools/payroll-advance-mixed-repayment-regression.js`
  (يُشغَّل بـ `node tools/payroll-advance-mixed-repayment-regression.js`) يُعيد هذا التحقق
  تلقائياً عبر `assert`، وسيفشل إن عاد الخلل القديم مستقبلاً.
- تم تحديث `tools/payroll-advance-repayments-regression.js`: استُبدل الفحص النصي الضعيف
  (`payment_type='salary'` موجود؟) بفحص يتأكد من **غياب** نمط الخلل تحديداً.
- ملاحظة جانبية غير متعلقة بهذا الإصلاح: كل من `tools/payroll-advances-regression.js` و
  `tools/payroll-advance-repayments-regression.js` يفشلان حالياً عند التشغيل بسبب فحص قديم
  يتوقع `CURRENT_SCHEMA_VERSION = 15` بينما المشروع تجاوز ذلك إلى v17 في تحديثات سابقة —
  هذا خلل موجود مسبقاً بملفات الفحص نفسها (غير ناتج عن هذه الجلسة) ويحتاج تحديثاً منفصلاً
  لرقم الإصدار المتوقع بها إن رغبتم.

## غير مُختبر (لا يزال)
- فتح `accounting.html`/`payroll.html` فعلياً بتطبيق Electron حقيقي على جهاز Windows —
  يوصى بتجربة سيناريو مطابق يدوياً على جهازك (صرف سلفة، تسديد مباشر جزئي لها، ثم تشغيل
  مسير رواتب لنفس الشهر) للتأكد.
