# إصلاح: جمود الواجهة أثناء الكتابة في نماذج مفتوحة (مثل الرواتب)

## المشكلة
عملية main.js فيها "حارس" (watchdog) يراقب نبضة حياة من الواجهة كل ثانيتين،
وإذا تأخرت النبضة أكثر من 12 ثانية كان يُعيد تحميل النافذة تلقائياً — إلا إذا
كانت شاشة الدفع مفتوحة أو كان هناك عنصر بصفة "busy" صريحة.

نماذج الرواتب (إضافة عامل، تسجيل سلفة/خصم/غياب...) لم تكن تضع أي صفة "busy"
أثناء الكتابة فيها، فإذا تأخر رد IPC للحظات كانت الصفحة تُعاد تحميلها فجأة
أثناء الكتابة — وهذا ما كان يظهر كـ"جمود" فجائي مع فقدان ما كُتب.

## الإصلاح
في `renderer/common.js`، أصبحت نبضة الحياة تعتبر المستخدم "مشغولاً" (busy)
أيضاً في حالتين إضافيتين، وليس فقط .loading/[aria-busy]:
- وجود أي نافذة منبثقة مفتوحة (.pro-modal أو .modal-overlay غير مخفية) —
  يشمل هذا كل نماذج الرواتب.
- وجود تركيز حالي (focus) على حقل إدخال/نص/قائمة منسدلة — أي المستخدم يكتب فعلياً.

هذا يمنع إعادة التحميل التلقائي طالما هناك نموذج مفتوح أو المستخدم يكتب،
بغض النظر عن الصفحة (وليس فقط شاشة الدفع كما كان سابقاً).

## الملفات المعدّلة
- renderer/common.js
